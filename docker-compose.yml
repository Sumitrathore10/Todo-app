version: "3.8"

services:
  client:
    build: ./client
    ports:
      - "5173:5173"
    networks:
      - mern
  server:
    build: ./server
    ports:
      - "8000:8000"
    networks: 
      - mern
    
  
networks:
  mern: 
    driver: bridge

volumes:
  data:
    driver: local
